<div data-controller="show-users">
  <!-- Options to admin -->

  <div class="menu d-flex mt-3">
    <button class="show-btn-admin" id="show-btn" data-action="click->show-users#show">
      <h2>Todos os profissionais</h2>
    </button>

    <button class="new-btn-admin" id="new-btn" data-action="click->show-users#new">
      <h2>Adicionar profissional</h2>
    </button>
  </div>

  <div data-show-users-target="newElement" class="d-none">
    <%= render 'shared/new', doctor: @doctor %>
  </div>

  <div data-show-users-target="showElement">
    <%= render 'shared/user_show', doctors: @doctors %>
  </div>

</div>
